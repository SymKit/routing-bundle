grumphp:
    tasks:
        phpstan:
            config: phpstan.neon.dist
            memory_limit: 512M
        phpcsfixer:
            config: .php-cs-fixer.dist.php
        phpunit:
            config: phpunit.xml.dist
        deptrac:
            config: deptrac.yaml
        infection:
            min_msi: 70
            min_covered_msi: 70
            config: infection.json5
